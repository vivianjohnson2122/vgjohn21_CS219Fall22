package InterfaceExamples;

public interface HW0_Specification {

    // return the sum of the digits in n
    int sod(int n); // everything is public by default so don't need to specify in method declaration
}
